# parser_log-files_service
A microservice that receives log files, analyzes them and aggregates information, stores the received data in PostgreSQL and provides a Rest API for retrieving information.
